---
{"dg-publish":true,"permalink":"/代码开发/Web 前端/0 前端基础/02 JavaScript/01 JavaScript 基础/04 作用域、作用域链、闭包基础/","dg-note-properties":{}}
---

---
---
`AO`和`GO`主要是用来解决「作用域」和「作用域链」相关所产生的一切问题。

`AO`和`function`相关，函数相当于一个独立的仓库，比如`A`仓库和`B`仓库。

## 🔢 作用域 & 作用域链
我们都知道在`JavaScript`中「一切皆对象」，每个对象都有自己的属性和方法。

```js
var obj = {
  name: "1",
  address: "1",
  teach: function () {},
};
console.log(obj.name);
```

而函数也是一种对象类型（引用类型）。

```js
function test(param1, param2) {}
console.log(test.name); // 函数的名字
console.log(test.length); // 形参的个数
```

在`JavaScript`中对象有一些属性是我们无法访问的，它们是`JS`引擎固有的「隐式属性」，是不供外界访问的（私有属性）。

那什么是作用域`[[scope]]`呢？？？

1. 函数「创建时生成的」一个`JS`内部的隐式属性
2. 函数存储作用域链的容器，作用域链存储的是`AO`和`GO`对象，每个函数的作用域链都是独立的！！！
    1. `AO`是函数执行期上下文对象
    2. `GO`是全局执行期上下文对象
3. `AO`是一个即时存储的容器，当函数执行完成之后，`AO`是要被销毁的，再次执行函数，`AO`会重新生成。

下面来看一个例子：

```js
function a() {
  function b() {
    var b = 2;
  }
  var a = 1;
  b();
}
var c = 3;
a();
```

以上以一段完整的代码，我们将代码逐步分解：

![画板](/img/user/%E4%BB%A3%E7%A0%81%E5%BC%80%E5%8F%91/Web%20%E5%89%8D%E7%AB%AF/0%20%E5%89%8D%E7%AB%AF%E5%9F%BA%E7%A1%80/02%20JavaScript/01%20JavaScript%20%E5%9F%BA%E7%A1%80/_assets/1734056893145-bacb047b-3faf-4dd5-85a0-725f15f6de3a.jpeg)

以上图解作用域链的时候，我们可以总结出：

- 函数被定义的时候就已经生成了`[[scope]]`，`[[scope]]`已经存储了`Scope Chain（作用域链）`，作用域链已经存储了`GO`；
- 函数执行的前一刻才生成的函数自己的`AO`；
- 函数的`[[scope]]`在父函数执行完成后才会销毁；

为什么函数的外部无法访问函数内部的变量呢？

从图中我们可以得知，作用域链是自上而下执行的，所以`b`函数可以访问`a`函数的变量，反过来则不行。

```js
function a() {
  function b() {
    var b = 2;
    console.log(a); // 1
  }
  var a = 1;
  b();
  console.log(b); // error
}
var c = 3;
a();
```

下面再来举个例子🌰 :

```js

function a() {
  function b() {
    function c() {}
    c();
  }
  b();
}
a();

// 以上代码的作用域链是：
/**
 * a定义：
 * a.[[scope]] > [GO]
 * a执行
 * a.[[scope]] > [AO,GO]
 * b定义
 * b.[[scope]] > [AO,GO]
 * b执行
 * b.[[scope]] > [AO,AO,GO]
 * c定义
 * c.[[scope]] > [AO,AO,GO]
 * c执行
 * c.[[scope]] > [AO,AO,AO,GO]
 */

/**
 * c结束：
 * c.[[scope]] > [AO,AO,AO,GO]
 * b结束：
 * b.[[scope]] > [AO,AO,GO]，c.[[scope]] 销毁
 * a结束：
 * a.[[scope]] > [AO,GO]，b.[[scope]] 销毁
 * 全局执行完成：
 * a.[[scope]] 也被销毁，只剩下 [GO]
 */
```

## 🔢 闭包
当内部函数被返回到外部并保存的时候，就一定会产生闭包

- 闭包会产生原来的作用域链不释放
- 过度的闭包可能会导致内存泄漏或加载过慢

举栗 🌰 ：

```js
function test1() {
  function test2() {
    var b = 2;
    console.log(a);
  }
  var a = 1;
  return test2;
}
var c = 3;
var test3 = test1();
test3();
```

![画板](/img/user/%E4%BB%A3%E7%A0%81%E5%BC%80%E5%8F%91/Web%20%E5%89%8D%E7%AB%AF/0%20%E5%89%8D%E7%AB%AF%E5%9F%BA%E7%A1%80/02%20JavaScript/01%20JavaScript%20%E5%9F%BA%E7%A1%80/_assets/1734060549155-091f2352-67dd-48a8-a42d-93f5bffaf17e.jpeg)

以上图解解释了什么是闭包。

举栗子🌰 ：

```js
function test() {
  var n = 100;

  function add() {
    n++;
  }
  function reduce() {
    n--;
    console.log(n);
  }

  return [add, reduce];
}
var arr = test();
arr[0](); // 101
arr[1](); // 100
```

因为`add`和`reduce`被保存到了外部执行，`test`函数的`AO`并没有被销毁，所以`add`和`reduce`操作的其实都是`test`函数的`n`。

同理看下面的例子：

```js
function breadMgr(num) {
  var breadNum = num || 10;

  function supply() {
    breadNum += 10;
    console.log(breadNum);
  }
  function sale() {
    breadNum--;
    console.log(breadNum);
  }

  return [supply, sale];
}
var breadMgr = breadMgr();
breadMgr[0](); // 20
breadMgr[1](); // 19
```

```js
function sunSched() {
  var sunSched = "";

  var operation = {
    setSched: function (thing) {
      sunSched = thing;
    },
    showSched: function () {
      console.log("My schedule on sunday is " + sunSched);
    },
  };
  return operation;
}
var sunSched = sunSched();
sunSched.setSched("studying");
sunSched.showSched(); // My schedule on sunday is studying
```
