---
{"dg-publish":true,"permalink":"/代码开发/Web 前端/0 前端基础/02 JavaScript/03 WebAPI/00 DOM 基础/15 移动端事件/","dg-note-properties":{}}
---

## 新增事件
在 PC 端开发的时候经常会使用到事件，到了移动端时代又新增了几个事件，分别是：

- touchstart：手指按下事件，类似 mousedown；
- touchmove：手指移动事件，类似 mousemove；
- touchend：手指抬起事件，类似 mouseup；
- touchcancel：系统中断当前触摸（如来电）时触发；

例如：

```html
<div class="container"></div>
```

```js
var box = document.querySelector('.container');
box.addEventListener('touchstart', () => {
  console.log('手指按下去了');
});
box.addEventListener('touchmove', () => {
  console.log('手指滑动了');
});
box.addEventListener('touchend', () => {
  console.log('手指抬起了');
});
```

![](/img/user/%E4%BB%A3%E7%A0%81%E5%BC%80%E5%8F%91/Web%20%E5%89%8D%E7%AB%AF/0%20%E5%89%8D%E7%AB%AF%E5%9F%BA%E7%A1%80/02%20JavaScript/03%20WebAPI/00%20DOM%20%E5%9F%BA%E7%A1%80/_assets/1750148749810-b7bf2e54-3c40-46e6-95d2-31cb0bd565f9.gif)

## 事件对象
在处理事件的时候会经常使用到事件对象，通过事件对象我们可以获取到关于本次事件更多的信息。

```js
box.addEventListener('touchstart', (e) => {
  console.log(e);
});
```

此时，我们打印移动端`touchstart`事件对象，可以看到如下的信息：

![](/img/user/%E4%BB%A3%E7%A0%81%E5%BC%80%E5%8F%91/Web%20%E5%89%8D%E7%AB%AF/0%20%E5%89%8D%E7%AB%AF%E5%9F%BA%E7%A1%80/02%20JavaScript/03%20WebAPI/00%20DOM%20%E5%9F%BA%E7%A1%80/_assets/1750148856053-30531c24-bbde-4a8d-abc8-90d2036cf44e.png)

可以看到事件对象里面有很多的属性，里面比较重要的三个属性分别是：

- `changedTouches`：触发当前事件的手指列表，也就是涉及当前（引发）事件的触摸点的列表；
- `targetTouches`：位于当前 DOM 元素上的手指列表，也就是当前对象上所有触摸点的列表；
- `touches`：位于当前屏幕上的所有手指列表（必需至少有 1 个手指在添加触发事件的元素上），也就是当前屏幕上所有触摸点的列表；

通过一个例子来区分一个触摸事件中的这三个属性：

![](/img/user/%E4%BB%A3%E7%A0%81%E5%BC%80%E5%8F%91/Web%20%E5%89%8D%E7%AB%AF/0%20%E5%89%8D%E7%AB%AF%E5%9F%BA%E7%A1%80/02%20JavaScript/03%20WebAPI/00%20DOM%20%E5%9F%BA%E7%A1%80/_assets/1750149042325-4256470c-8344-4eba-aeea-9b357e1e315e.jpeg)

比如有两个元素 div1 和 div2，只有 div2 绑定了`touchstart`事件，第一次放下一个手指在 div2 上，触发了`touchstart`事件，这个时候，三个集合的内容是一样的，都包含这个手指的 touch。

然后，再放下两个手指一个在 div1 上，一个在 div2 上，这个时候又会触发事件，但`changedTouches`属性里面只包含第二个第三个手指的信息，因为第一个没有发生变化，而`targetTouches`包含的是在第一个手指和第三个在 div2 上的手指集合，touches 包含屏幕上所有手指的信息，也就是三个手指。

## 移动端事件和 PC 端事件的区别？
虽然说移动端有了专属的事件，也并不意味着原本的 PC 端浏览器事件就不能用了，但是还是推荐尽量使用移动端专属事件，因为 PC 端的事件并不是专门为移动端设计的，因此会存在各种各样的问题，其中一个比较出名的问题就是`click`300ms 延迟。

为什么`click`事件会存在 300ms 延迟呢？

早在 2007 年初，苹果公司在发布首款 iPhone 前夕，遇到一个问题：当时的网站都是为大屏幕设备所设计的。于是苹果的工程师们做了一些约定，应对 iPhone 这种小屏幕浏览桌面端站点的问题。

这当中最出名的，当属双击缩放（ double tap to zoom ），这也是会有上述 300 毫秒延迟的主要原因。

双击缩放，顾名思义，即用手指在屏幕上快速点击两次，iOS 自带的 Safari 浏览器会将网页缩放至原始比例。

那么这和 300 毫秒延迟有什么联系呢？

假定这么一个场景： 用户在 iOS Safari 里边点击了一个链接。由于用户可以进行双击缩放或者双击滚动的操作，当用户一次点击屏幕之后，浏览器并不能立刻判断用户是确实要打开这个链接，还是想要进行双击操作。因此，iOS Safari 就等待 300 毫秒，以判断用户是否再次点击了屏幕。

鉴于 iPhone 的成功，其他移动浏览器都复制了 iPhone Safari 浏览器的多数约定，包括双击缩放，几乎现在所有的移动端浏览器都有这个功能。

下面我们就来复现这个问题：

```js
var box = document.querySelector('.container');
box.addEventListener('click', () => {
  console.log('click 事件触发');
});
box.addEventListener('touchstart', () => {
  console.log('touchstart 事件触发');
});
```

> [!warning]
>
> 在 2014 年，从 Chrome32 开始 Google 已经解决这个 300ms 延迟问题，只要定义了`<viewport />`就不会有 300ms 延迟问题。
>
> ![](/img/user/%E4%BB%A3%E7%A0%81%E5%BC%80%E5%8F%91/Web%20%E5%89%8D%E7%AB%AF/0%20%E5%89%8D%E7%AB%AF%E5%9F%BA%E7%A1%80/02%20JavaScript/03%20WebAPI/00%20DOM%20%E5%9F%BA%E7%A1%80/_assets/1750149489495-b996afd8-d99c-437e-be02-0945112fab0b.png)


效果如下：

![](/img/user/%E4%BB%A3%E7%A0%81%E5%BC%80%E5%8F%91/Web%20%E5%89%8D%E7%AB%AF/0%20%E5%89%8D%E7%AB%AF%E5%9F%BA%E7%A1%80/02%20JavaScript/03%20WebAPI/00%20DOM%20%E5%9F%BA%E7%A1%80/_assets/1750149584049-3c581b54-a015-45a3-bc67-e38c50f69537.gif)

可以很明显的看到，`click`事件的执行回延迟一段时间。

除此之外，移动端事件和 PC 端事件还有哪些区别呢？

- 触发点不同；
- 触发顺序不同；
- `touchstart`和`click`的区别；

1、触发点不同。

PC 端：

- `mousemove`：不需要鼠标按下，但是「必需在元素上」才能触发；
- `mouseup`：必需在元素上抬起才能触发；

移动端：

- `touchmove`：必需手指按下才能触发，但是按下后不在元素上也能触发；
- `touchend`：不需要在元素上抬起就能触发；

2、触发顺序不同。

触发顺序依次为：`touchstart` → `touchend` → `mousedown` → `click` → `mouseup`。

并且 PC 端的事件在移动端会有 300ms 延迟。

3、`touchstart`和`click`的区别。

`touchstart`为手指碰到元素就触发，`click`为手指碰到元素并且抬起才会触发。

## 事件穿透
所谓事件穿透是移动端开发中非常有名的一个 Bug，其场景是：有两层重叠的元素，上面的元素有`touch`事件（点击后要消失），下面是一个默认会触发`click`事件的元素（`<a>`元素、表单元素、带`click`事件的元素），此时点击上一层的元素，下一层也同样会被点击。

示例：

```css
* {
  margin: 0;
  padding: 0;
}
.container {
  width: 200px;
  height: 200px;
  background: green;
  color: #fff;
  position: absolute;
  left: 0;
  top: 0;
  opacity: 0.5;
}
input {
  border: 1px solid #000;
}
```

```html
<a href="http://www.baidu.com/">度娘</a><br>
<input type="text">
<p>这是一个段落</p>
<div class="container"></div>
```

这样就形成下面的结构：

![](/img/user/%E4%BB%A3%E7%A0%81%E5%BC%80%E5%8F%91/Web%20%E5%89%8D%E7%AB%AF/0%20%E5%89%8D%E7%AB%AF%E5%9F%BA%E7%A1%80/02%20JavaScript/03%20WebAPI/00%20DOM%20%E5%9F%BA%E7%A1%80/_assets/1750150297163-8d0574e9-d74f-4a36-8705-6a1c65ff6b39.png)

重点关注 JS 的事件绑定：

```js
var box = document.querySelector('.container');

box.addEventListener('touchstart', () => {
  box.style.display = 'none';
});

const p = document.querySelector('p');
p.addEventListener('click', () => {
  alert('该段落被点击了');
});
```

![](/img/user/%E4%BB%A3%E7%A0%81%E5%BC%80%E5%8F%91/Web%20%E5%89%8D%E7%AB%AF/0%20%E5%89%8D%E7%AB%AF%E5%9F%BA%E7%A1%80/02%20JavaScript/03%20WebAPI/00%20DOM%20%E5%9F%BA%E7%A1%80/_assets/1750151601301-abf0e805-c598-42d6-b520-d231e0f803cb.gif)

可以看到，当我第一次点击 box 后，控制台直接输出了“该段落被点击了”，第二次点击 box 后页面直接跳转到了百度。

那么为什么会出现事件穿透呢？

究其原因还是因为在移动端中，事件的执行顺序是`touchstart` → `touchend` → `mousedown` → `click` → `mouseup`。

而`click`事件又存在 300ms 的延迟，当`touchstart`事件把上层元素隐藏之后，隔了 300ms 后，浏览器触发了`click`事件，但是此时上层元素不见了，所以该事件就被派发到了下层元素上。

要解决这个问题，有下面几种方案：

1、直接禁用`click`事件。

将页面内所有元素的`click`事件改用`touch`事件。这种方法的好处非常明显，既解决了`click`事件延迟造成体验不佳的问题又解决了事件穿透的问题，但是缺点也很明显，就是`<a>`标签的链接跳转的处理问题。

```js
document.addEventListener('click', (e) => {
  const href = e.target.getAttribute('href')
  const nodeName = e.target.nodeName.toLowerCase()

  if (nodeName === 'a' && href) {
    e.preventDefault()
  }
})

document.addEventListener('touchstart', (e) => {
  const href = e.target.getAttribute('href')
  const nodeName = e.target.nodeName.toLowerCase()

  if (nodeName === 'a' && href) {
    const target = e.target.getAttribute('target')
    window.open(href, target || '_self')
  }
})
```

然而，当`<a>`标签内包含后代元素的时候，后代元素的`click`事件通过冒泡还是会触发`<a>`标签的跳转。

```html
<a href="/home">
  <span>点击我跳转</span>
</a>
```

```js
// 条件不成立，a 元素正常跳转
if (nodeName === 'a' && href) {
  e.preventDefault()
}
```

使用`pointer-events`禁用`<a>`标签所有后代元素的鼠标事件：

```css
a[href] * {
  pointer-events: none;
}
```

2、在弹窗或者操作去后面添加一个透明的遮罩层，拦截`click`的触发。

```html
<div class="mask" id="click-mask"></div>
```

```css
.mask {
  position: fixed;
  top: 0; left: 0;
  width: 100%;
  height: 100%;
  z-index: 9999;
  background: transparent; /* 完全透明 */
}
```

并在`touch`事件后延迟 300ms 后隐藏：

```js
box.addEventListener("touchstart", () => {
  box.style.display = "none";

  setTimeout(() => {
    mask.style.display = 'none'
  }, 300)
});
```

3、取消`touch`事件的默认行为。

这也是最简单的解决办法。

```js
box.addEventListener('touchstart', ev => {
  box.style.display = 'none';
  ev.preventDefault(); // 取消事件的默认动作
});
```

## 阻止默认行为的影响
在移动端，不仅元素身上绑定了默认事件，在`document`上也绑定了默认事件，因此我们可以利用事件冒泡来阻止默认事件。

也就是说，哪怕你元素本身没有阻止默认事件，你触发了默认事件，但是通过冒泡到父元素身上取消了默认事件，那么最终的结果默认事件也会被取消掉。因为根据事件流，元素身上的事件在目标阶段触发，冒泡事件在冒泡阶段触发，冒泡阶段在目标阶段之后，最终结果取决于后执行的事件。

利用这个思想我们就可以通过在`document`身上取消默认事件，从而阻止所有的默认事件。

```js
document.addEventListener('touchstart', ev => {
  ev.preventDefault();
});
```

虽然我们阻止了所有的默认事件，但是这种方式也带来了新的问题：

`touchmove`：

- 阻止了浏览器的滚动条；
- 阻止了用户双指缩放；

touchstart：

- 解决 iOS 10+ 及部分安卓机通过设置`<viewport />`禁止用户缩放的功能（双指滑动、双击）;
- 解决事件点透问题；
- 阻止图片文字被选中；
- 阻止了长按元素会弹出系统菜单；
- 阻止了浏览器回弹的效果；
- 阻止了浏览器的滚动条；
- 阻止了鼠标的事件；
- 阻止了`<input />`框的输入功能；

我们可以通过下面的示例来看到部分功能失去了效果：

```css
body {
  margin: 0;
}

ul {
  margin: 0;
  padding: 0;
  list-style: none;
}

li {
  line-height: 50px;
}

li a {
  font-size: 30px;
}

input {
  border: 1px solid #000;
}
```

```html
<div class="page">
  <img src="./template.jpeg" alt="" width="100%" />
  <input type="text" />
  <ul>
    <li><a href="http://www.baidu.com">度娘</a></li>
    <li><a href="#">这是一个很长很长的链接</a></li>
    <li><a href="#">这是一个很长很长的链接</a></li>
    <li><a href="#">这是一个很长很长的链接</a></li>
    <li><a href="#">这是一个很长很长的链接</a></li>
    <li><a href="#">这是一个很长很长的链接</a></li>
    <li><a href="#">这是一个很长很长的链接</a></li>
    <li><a href="#">这是一个很长很长的链接</a></li>
    <li><a href="#">这是一个很长很长的链接</a></li>
    <li><a href="#">这是一个很长很长的链接</a></li>
    <li><a href="#">这是一个很长很长的链接</a></li>
    <li><a href="#">这是一个很长很长的链接</a></li>
    <li><a href="#">这是一个很长很长的链接</a></li>
    <li><a href="#">这是一个很长很长的链接</a></li>
    <li><a href="#">这是一个很长很长的链接</a></li>
    <li><a href="#">这是一个很长很长的链接</a></li>
    <li><a href="#">这是一个很长很长的链接</a></li>
    <li><a href="#">这是一个很长很长的链接</a></li>
    <li><a href="#">这是一个很长很长的链接</a></li>
    <li><a href="#">这是一个很长很长的链接</a></li>
    <li><a href="#">这是一个很长很长的链接</a></li>
  </ul>
</div>
```

```js
const page = document.querySelector(".page");
page.addEventListener("touchstart", (ev) => {
  ev.preventDefault();
});
```

![](/img/user/%E4%BB%A3%E7%A0%81%E5%BC%80%E5%8F%91/Web%20%E5%89%8D%E7%AB%AF/0%20%E5%89%8D%E7%AB%AF%E5%9F%BA%E7%A1%80/02%20JavaScript/03%20WebAPI/00%20DOM%20%E5%9F%BA%E7%A1%80/_assets/1750154244169-d1f53987-9f05-4633-8ed6-de6002ee0eb4.gif)

可以看到，对`touchstart`事件阻止默认行为后，在移动端中长按图片也不会触发保存的菜单了，点击`<a>`元素也不会跳转了。

但是同样的代码，我们将调试模式调会至 PC 端后会发现默认行为并没有成功阻止。

![](/img/user/%E4%BB%A3%E7%A0%81%E5%BC%80%E5%8F%91/Web%20%E5%89%8D%E7%AB%AF/0%20%E5%89%8D%E7%AB%AF%E5%9F%BA%E7%A1%80/02%20JavaScript/03%20WebAPI/00%20DOM%20%E5%9F%BA%E7%A1%80/_assets/1750154422712-1f2e0286-ec59-48dd-ba54-d71124368860.png)

其中`preventDefault`不生效的原因是由`passive`这个参数引起的。`addEventListener()`方法还可以传递第三个参数，该参数是一个对象，[详见](https://developer.mozilla.org/en-US/docs/Web/API/EventTarget/addEventListener#options)：

```plain
tartget.addEventListener(type, listener, {
  capture: Booolean,
  once: Boolean,
  passive: Boolean,
  signal: AbortSignal
})
```

为什么？

事件监听器在监听事件的时候，并不能提前知道回调函数中是否对默认行为进行了阻止，因此若想知道是否会阻止就需要等待函数执行完成后，这段时间虽然很短，但是仍然会感觉到卡顿。

![](/img/user/%E4%BB%A3%E7%A0%81%E5%BC%80%E5%8F%91/Web%20%E5%89%8D%E7%AB%AF/0%20%E5%89%8D%E7%AB%AF%E5%9F%BA%E7%A1%80/02%20JavaScript/03%20WebAPI/00%20DOM%20%E5%9F%BA%E7%A1%80/_assets/1750154666058-c9590e98-77cf-412c-beda-7cb0e5944135.gif)

于是，我们就可以通过`passive`参数来告诉浏览器事件处理程序不会调用`preventDefault()`来阻止默认行为。而大部分事件监听器是不会阻止默认行为的，因此某些浏览器就将一些节点（例如`document`）事件的`passive`默认设置为为`true`。

所以，要解决上面`document`无法阻止默认行为的问题，只需要将`passive`设置为`false`即可，明确告诉浏览器，事件处理程序会调用`preventDefault()`来阻止默认行为。

```js
document.addEventListener('touchstart', ev => {
	ev.preventDefault();
}, { passive: false });
```

回到上面的话题，如果在父元素上面阻止了元素的默认行为，就会导致很多元素的默认行为也没有了，那么此时怎么办呢？

例如获取验证码的时候，验证码通常用户是希望能够复制的，因此会触发长按复制的默认事件，但是这个事件我们希望在这个元素上不被阻止，应该如何实现呢？

有一种方案是阻止冒泡，如上面所述，我们是通过冒泡来借助父元素阻止掉默认事件，那么如果我们切断冒泡，那么父元素身上的阻止默认事件就无法被触发，元素本身的默认事件就无法被阻止掉了。

但是这样也只是解决了其中一个问题，上面还罗列出了很多其他问题，例如所有的滚动条失效，这些问题该怎么解决呢？

实际上，移动端进行开发时一上来就需要阻止所有的默认事件，后面的很多功能都需要我们自定义来实现。
